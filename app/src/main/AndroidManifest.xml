<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Do_an"
        tools:targetApi="31">
        <activity
            android:name=".ui.ThongbaoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ChuyenTienActivity"
            android:exported="false" />
        <activity
            android:name=".ui.RutTienActivity"
            android:exported="false" />
        <activity
            android:name=".ui.NapTienActivity"
            android:exported="false" />
        <activity
            android:name=".ui.LockAccountActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ChangePasswordActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ContactInfoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.PersonalInfoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.LoginAndSecurityActivity"
            android:exported="false" />
        <activity
            android:name=".ui.PersonalPageActivity"
            android:exported="false" />
        <activity
            android:name=".ui.EnterSdtActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity -->
        <!-- android:name=".ui.EditChiTietLichActivity" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".ui.ChiTietLichActivity" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".admin.InfoUserActivityAdmin" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".admin.AllUsersActivity" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".admin.AllLichActivity" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".admin.AdminActivity" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".ui.EditCarActivity" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".ui.DatLichActivity" -->
        <!-- android:exported="true" /> -->
        <!-- <activity -->
        <!-- android:name=".ui.EditUserInfoActivity" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".ui.InfoUserActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".ui.QuenPassActivity"
            android:exported="true" />
        <activity
            android:name=".ui.Register2Activity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true" />
    </application>

</manifest>